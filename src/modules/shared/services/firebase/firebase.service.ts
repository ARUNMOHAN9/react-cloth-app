import { _Firebase as Firebase } from '../../configs/firebase/firebase';
import { _FirebaseContext as FirebaseContext } from '../../contexts/firebase/FirebaseContext';

export default Firebase;

export { FirebaseContext };